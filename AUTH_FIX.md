# Исправления авторизации через Telegram

## Что было исправлено:

### 1. Обработка Deep Link в `electron/main.js`
- Улучшен парсинг URL для deep link (`cloakvpn://auth?data=...`)
- Добавлена обработка различных форматов URL
- Добавлено логирование для отладки
- Добавлена обработка отложенных deep link (когда окно еще не создано)

### 2. Обработка авторизации в `src/components/TelegramAuth.tsx`
- Улучшена функция `handleAuthFromDeepLink`:
  - Добавлено подробное логирование на каждом этапе
  - Улучшена обработка ошибок
  - Добавлен fallback для использования данных из `authData.user` если профиль не найден
  - Улучшена проверка наличия токенов и данных пользователя

### 3. Удалена локальная база данных
- Удален файл `electron/database.js`
- Удалены все IPC handlers для работы с локальной БД
- Теперь используется только Supabase клиент

## Как работает авторизация:

1. Пользователь нажимает "Войти через Telegram" в приложении
2. Открывается Telegram бот `@cloakv_bot`
3. Пользователь нажимает "✅ Войти" в боте
4. Открывается сайт для авторизации
5. После авторизации пользователь нажимает "🚀 Открыть CloakVPN" в боте
6. Открывается deep link `cloakvpn://auth?data=...` с данными авторизации
7. Приложение получает данные, устанавливает сессию Supabase и авторизует пользователя

## Что нужно проверить в Supabase:

### 1. Таблица `profiles`
Убедитесь, что таблица `profiles` существует и имеет следующие поля:
- `id` (uuid, primary key, references auth.users)
- `email` (text)
- `name` (text)
- `telegram_id` (bigint, unique)
- `telegram_username` (text, nullable)
- `subscription_plan` (text, default 'free')
- `subscription_expires_at` (timestamp, nullable)
- `subscription_is_active` (boolean, default true)
- `created_at` (timestamp, default now())
- `updated_at` (timestamp, default now())

### 2. Row Level Security (RLS)
Убедитесь, что RLS включен для таблицы `profiles` и есть политики:
- SELECT: пользователи могут читать свой профиль
- UPDATE: пользователи могут обновлять свой профиль
- INSERT: автоматически создается при регистрации

### 3. Триггер для создания профиля
Убедитесь, что есть триггер, который автоматически создает запись в `profiles` при создании пользователя в `auth.users`.

SQL для триггера:
```sql
-- Сначала удаляем существующий триггер, если он есть
DROP TRIGGER IF EXISTS on_auth_user_created ON auth.users;

-- Создаем или заменяем функцию
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS trigger AS $$
BEGIN
  INSERT INTO public.profiles (id, email, name)
  VALUES (
    NEW.id,
    NEW.email,
    COALESCE(NEW.raw_user_meta_data->>'name', 'User')
  );
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

-- Создаем триггер
CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION public.handle_new_user();
```

### 4. Переменные окружения
Убедитесь, что в `.env` файле установлены:
```
VITE_SUPABASE_URL=https://ncjadpqxkekqprnvsbee.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_4eMQNthf5nOt-riF9Vm2aQ_Zvfdw5bk
VITE_TELEGRAM_BOT_USERNAME=cloakv_bot
VITE_API_URL=https://cloak-vpn.vercel.app
```

## Отладка:

Если авторизация не работает, проверьте консоль браузера (DevTools) - там будут логи:
- `handleDeepLink: received URL` - получен deep link
- `handleAuthFromDeepLink: received authData` - получены данные авторизации
- `Setting Supabase session` - установка сессии
- `Supabase session set successfully` - сессия установлена
- `User profile loaded` - профиль загружен
- `User saved to localStorage` - пользователь сохранен

## Возможные проблемы:

1. **Deep link не открывается**: Проверьте, что протокол `cloakvpn://` зарегистрирован в системе
2. **Сессия не устанавливается**: Проверьте, что токены в `authData.session` валидны
3. **Профиль не найден**: Проверьте, что триггер создает профиль при регистрации пользователя
4. **RLS блокирует доступ**: Проверьте политики безопасности в Supabase

