# 🚀 Инструкция по деплою CloakVPN

## Подготовка к деплою

### 1. Проверка конфигурации

Убедитесь, что все файлы настроены правильно:
- ✅ `package.json` - версия и скрипты
- ✅ `electron-builder.config.js` - конфигурация сборки
- ✅ `.github/workflows/build.yml` - автоматическая сборка

### 2. Локальная сборка (тестирование)

Перед деплоем протестируйте сборку локально:

```bash
# Установка зависимостей
npm install

# Сборка React приложения
npm run build

# Сборка EXE файла
npm run electron:build
```

Результат будет в папке `release/`:
- `CloakVPN-1.0.0-portable.exe` - Portable версия для Windows

## Деплой в GitHub

### Способ 1: Автоматический деплой через GitHub Actions

1. **Создайте репозиторий на GitHub:**
   - Перейдите на https://github.com/Rew1x3/CloakVPN_Programm
   - Или создайте новый репозиторий

2. **Инициализируйте Git (если еще не сделано):**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```

3. **Подключите удаленный репозиторий:**
   ```bash
   git remote add origin https://github.com/Rew1x3/CloakVPN_Programm.git
   git branch -M main
   git push -u origin main
   ```

4. **Создайте релиз:**
   - Перейдите в раздел "Releases" на GitHub
   - Нажмите "Create a new release"
   - Создайте новый тег (например, `v1.0.0`)
   - GitHub Actions автоматически соберет и загрузит EXE файл

### Способ 2: Ручной деплой

1. **Соберите приложение локально:**
   ```bash
   npm run electron:build
   ```

2. **Создайте релиз на GitHub:**
   - Перейдите в раздел "Releases"
   - Нажмите "Draft a new release"
   - Загрузите EXE файл из папки `release/`

## Структура релиза

После сборки в папке `release/` будут следующие файлы:

```
release/
├── CloakVPN-1.0.0-portable.exe  # Portable версия (не требует установки)
└── builder-debug.yml            # Лог сборки (можно удалить)
```

## Важные замечания

### Portable EXE файл

- ✅ Не требует установки
- ✅ Можно запускать напрямую
- ✅ Все зависимости включены в файл
- ✅ Размер файла будет больше (~100-200 MB)

### Иконка приложения

Если у вас нет иконки, создайте папку `build/` и добавьте:
- `build/icon.ico` - для Windows
- `build/icon.icns` - для macOS
- `build/icon.png` - для Linux

Или удалите строки с `icon` из конфигурации.

## Обновление версии

Для создания нового релиза:

1. Обновите версию в `package.json`:
   ```json
   "version": "1.0.1"
   ```

2. Создайте коммит:
   ```bash
   git add package.json
   git commit -m "Bump version to 1.0.1"
   git push
   ```

3. Создайте новый тег:
   ```bash
   git tag v1.0.1
   git push origin v1.0.1
   ```

4. GitHub Actions автоматически соберет новую версию

## Устранение проблем

### Ошибка при сборке

Если сборка не работает:
1. Убедитесь, что все зависимости установлены: `npm install`
2. Проверьте конфигурацию в `electron-builder.config.js`
3. Проверьте логи в консоли

### EXE файл не запускается

1. Проверьте, что файл не заблокирован антивирусом
2. Убедитесь, что у вас Windows 10/11 (64-bit)
3. Попробуйте запустить от имени администратора

### GitHub Actions не работает

1. Проверьте, что файл `.github/workflows/build.yml` существует
2. Убедитесь, что используется правильный синтаксис YAML
3. Проверьте логи в разделе "Actions" на GitHub

## Полезные команды

```bash
# Разработка
npm run dev                    # Запуск web версии
npm run electron:dev          # Запуск Electron приложения

# Сборка
npm run build                 # Сборка React приложения
npm run electron:build        # Сборка EXE файла
npm run electron:pack         # Сборка без упаковки (для теста)

# Git
git status                    # Проверить статус
git add .                     # Добавить все файлы
git commit -m "Message"       # Создать коммит
git push                      # Отправить на GitHub
```

---

**Успешного деплоя! 🚀**


